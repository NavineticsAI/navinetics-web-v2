# Global Rules

- **GitHub Pushes**: NEVER push to GitHub or execute remote write commands (e.g., `git push`, `gh pr review --approve`) unless the user EXPLICITLY states to do so. All code changes and commits must remain local unless a push is directly requested.
